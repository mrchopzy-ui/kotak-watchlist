
import requests
import zipfile
import io
import csv
import os
from datetime import datetime, timedelta

OUTPUT_FILE = "data/nse_company_master.csv"

os.makedirs("data", exist_ok=True)

def download_latest_bhavcopy():
    # NSE blocks same-day downloads sometimes → try last 5 days
    for i in range(1, 6):
        date = datetime.now() - timedelta(days=i)
        d = date.strftime("%d%m%Y")
        url = f"https://archives.nseindia.com/content/historical/EQUITIES/{date.year}/{date.strftime('%b').upper()}/cm{d}bhav.csv.zip"

        headers = {
            "User-Agent": "Mozilla/5.0",
            "Accept": "*/*",
            "Referer": "https://www.nseindia.com"
        }

        r = requests.get(url, headers=headers, timeout=10)
        if r.status_code == 200:
            return r.content

    raise Exception("❌ Unable to download NSE bhavcopy")

print("⬇️ Downloading NSE bhavcopy...")
zip_data = download_latest_bhavcopy()

print("📦 Extracting data...")
z = zipfile.ZipFile(io.BytesIO(zip_data))
csv_name = z.namelist()[0]

rows = csv.DictReader(io.TextIOWrapper(z.open(csv_name), encoding="utf-8"))

companies = {}

for r in rows:
    if r["SERIES"] == "EQ":
        companies[r["SYMBOL"]] = r["NAME OF COMPANY"].strip()

print(f"💾 Writing {len(companies)} companies...")

with open(OUTPUT_FILE, "w", newline="", encoding="utf-8") as f:
    w = csv.writer(f)
    w.writerow(["symbol", "company_name"])
    for k, v in sorted(companies.items()):
        w.writerow([k, v])

print("✅ DONE")
print(f"📁 Saved to {OUTPUT_FILE}")
